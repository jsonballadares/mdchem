using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class StatusTextDefault : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        // Debug.Log("THE NAME OF THE TEXT " + this.gameObject.name);
        // this.gameObject.GetComponent<TMPro.TMP_Text>().text = "";
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
